# Navegación por hubs — plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bajar la barra de navegación de 9 enlaces a 4 categorías, cada una con
su página intermedia, sin romper ninguna ruta ni ningún enlace compartido.

**Architecture:** Una tabla única de herramientas (`js/tools.js`) alimenta las
tarjetas del home, el listado de cada hub y el mapa de pestaña activa. `js/hub.js`
pinta cualquier categoría a partir de esa tabla. El router gana dos rutas
(`#/data`, `#/competitive`) y deja de nombrar la pestaña activa a mano en cada
rama.

**Tech Stack:** JavaScript de módulos ES sin build, servido estático. Sin
dependencias. Verificación con scripts `node scripts/check-*.mjs` y con el
navegador.

## Global Constraints

- **Spec:** `docs/superpowers/specs/2026-08-05-nav-hubs-design.md`.
- **Rama:** `feat/pokedex-expansion`. **Nunca hacer push**; Álvaro sube.
- **Commits:** conventional commits, mensajes sencillos, **sin ninguna
  atribución** (`Co-Authored-By`, `Generated with` ni trailers). Usar
  `git -c commit.gpgsign=false commit` si la firma da problemas.
- **Un commit por tarea**, atómico, antes de empezar la siguiente.
- **Cero datos nuevos.** No se toca `data/` ni `scripts/build-data.mjs`.
- **Las 12 rutas actuales siguen respondiendo** con su estado en la URL intacto:
  `/`, `/pokedex`, `/pokedex/:id`, `/moves`, `/moves/:id`, `/abilities`,
  `/abilities/:name`, `/items`, `/natures`, `/types`, `/team`, `/calculator`.
- **Regla de ubicación medida:** hasta 3 herramientas, pestañas; a partir de 4,
  hub con tarjetas. La calculadora conserva sus 3 pestañas.
- **Paridad hub ↔ home:** toda herramienta tiene tarjeta en el home y aparece en
  su hub (o es el destino directo de su categoría, si es la única).
- **Verificar en el navegador con recarga real.** Cambiar solo el fragmento de
  la URL no recarga el documento y se sigue ejecutando el módulo viejo: usar
  `location.reload()` o pasar por otra URL. Servir con
  `node scripts/serve.mjs 8095`, nunca `python3 -m http.server`.
- **Textos en dos idiomas.** Toda etiqueta nueva necesita su clave en `es` y en
  `en` dentro de `js/i18n.js`.

---

### Task 1: La tabla de herramientas

**Files:**
- Create: `js/tools.js`
- Create: `scripts/check-tools.mjs`
- Modify: `js/i18n.js` (bloque `es` y bloque `en`)

**Interfaces:**
- Produces:
  - `CATEGORIES: Array<{id, route, label, direct?: boolean}>`
  - `TOOLS: Array<{id, category, route, base, icon, label, desc}>`
  - `toolsIn(categoryId) -> Tool[]`
  - `categoryOf(path) -> string` — `''` si ninguna coincide
  - `targetOf(categoryId) -> string` — la ruta a la que enlaza la pestaña

- [ ] **Step 1: Escribir el check que falla**

Crear `scripts/check-tools.mjs`:

```js
// Comprueba la tabla de herramientas: que no haya ids ni rutas repetidas, que
// cada categoria tenga destino, y que ninguna etiqueta se quede sin traducir en
// alguno de los dos idiomas.
// Run with: node scripts/check-tools.mjs
import { readFile } from 'node:fs/promises';
import { CATEGORIES, TOOLS, toolsIn, categoryOf, targetOf } from '../js/tools.js';

let failed = 0;

function check(label, actual, expected) {
  const ok = JSON.stringify(actual) === JSON.stringify(expected);
  if (!ok) failed++;
  console.log(`${ok ? '  ok  ' : '  FAIL'} ${label}: ${JSON.stringify(actual)}${ok ? '' : ` (expected ${JSON.stringify(expected)})`}`);
}

console.log('\nEstructura\n');

check('hay 4 categorias', CATEGORIES.length, 4);
check('ids de categoria', CATEGORIES.map(c => c.id), ['pokedex', 'data', 'competitive', 'calculator']);
check('ids de herramienta sin repetir', TOOLS.length, new Set(TOOLS.map(t => t.id)).size);
check('rutas sin repetir', TOOLS.length, new Set(TOOLS.map(t => t.route)).size);
check('toda herramienta tiene categoria conocida',
  TOOLS.filter(t => !CATEGORIES.some(c => c.id === t.category)).map(t => t.id), []);
check('toda herramienta tiene icono, etiqueta y descripcion',
  TOOLS.filter(t => !t.icon || !t.label || !t.desc).map(t => t.id), []);
check('toda categoria tiene al menos una herramienta',
  CATEGORIES.filter(c => toolsIn(c.id).length === 0).map(c => c.id), []);

console.log('\nLa pestana activa sale de la ruta\n');

check('la lista de movimientos es Datos', categoryOf('/moves'), 'data');
check('la ficha de un movimiento tambien', categoryOf('/moves/53'), 'data');
check('la ficha de un Pokemon es Pokedex', categoryOf('/pokedex/6'), 'pokedex');
check('el equipo es Competitivo', categoryOf('/team'), 'competitive');
check('la calculadora es Calculadora', categoryOf('/calculator'), 'calculator');
check('las naturalezas son Datos', categoryOf('/natures'), 'data');
check('el home no enciende ninguna', categoryOf('/'), '');
check('una ruta inventada tampoco', categoryOf('/nonsense'), '');

// El hub de una categoria enciende su propia pestana. Ninguna herramienta tiene
// base 'data' ni 'competitive', asi que sin esto la pestana se apagaria justo
// en la pagina de la categoria.
check('el hub de Datos enciende Datos', categoryOf('/data'), 'data');
check('el hub de Competitivo enciende Competitivo', categoryOf('/competitive'), 'competitive');

console.log('\nUna categoria con una sola herramienta va directa\n');

// Hoy Pokedex y Competitivo tienen una sola herramienta: la pestana lleva a
// ella y no a un hub que no ensenaria nada. Cuando el sub-bloque 4 anada las
// suyas, estas dos comprobaciones cambian a '#/pokedex' y '#/competitive'.
check('Pokedex va directa a su lista', targetOf('pokedex'), '#/pokedex');
check('Competitivo va directo a Equipo', targetOf('competitive'), '#/team');
check('Datos abre su hub', targetOf('data'), '#/data');
check('Calculadora abre su pagina de pestanas', targetOf('calculator'), '#/calculator');

console.log('\nTraducciones\n');

const i18n = await readFile(new URL('../js/i18n.js', import.meta.url), 'utf8');

// Cada clave tiene que aparecer dos veces: una por idioma. El home y los hubs
// usan hub.<categoria>.title y .subtitle para LAS CUATRO categorias, calculadora
// incluida, asi que entran todas en la cuenta.
const claves = [...new Set([
  ...TOOLS.flatMap(tool => [tool.label, tool.desc]),
  ...CATEGORIES.map(c => c.label),
  ...CATEGORIES.flatMap(c => [`hub.${c.id}.title`, `hub.${c.id}.subtitle`]),
])];
const sinTraducir = claves.filter(k =>
  (i18n.match(new RegExp(`'${k.replace(/\./g, '\\.')}'\\s*:`, 'g')) || []).length < 2);
check('toda clave existe en los dos idiomas', sinTraducir, []);

console.log(failed ? `\n${failed} check(s) failed\n` : '\nAll checks passed\n');
process.exit(failed ? 1 : 0);
```

- [ ] **Step 2: Ejecutarlo para verlo fallar**

Run: `node scripts/check-tools.mjs`
Expected: FAIL — `Cannot find module '../js/tools.js'`.

- [ ] **Step 3: Añadir las claves nuevas a `js/i18n.js`**

En el bloque `es`, junto a las demás `nav.*`:

```js
    'nav.data': 'DATOS',
    'nav.competitive': 'COMPETITIVO',
    'hub.data.title': 'Datos',
    'hub.data.subtitle': 'Las tablas del juego: movimientos, habilidades, objetos, naturalezas y tipos',
    'hub.competitive.title': 'Competitivo',
    'hub.competitive.subtitle': 'Herramientas para construir y medir un equipo',
    'hub.pokedex.title': 'Pokedex',
    'hub.pokedex.subtitle': 'Las 1025 especies y las herramientas que salen de ellas',
    'hub.calculator.title': 'Calculadora',
    'hub.calculator.subtitle': 'IV/EV, dano y captura',
```

`hub.calculator.*` hace falta aunque la calculadora no tenga hub: el home usa
esa misma clave como encabezado de su grupo, y sin ella saldría la clave cruda
en pantalla.

En el bloque `en`, en el mismo sitio:

```js
    'nav.data': 'DATA',
    'nav.competitive': 'COMPETITIVE',
    'hub.data.title': 'Data',
    'hub.data.subtitle': 'The game tables: moves, abilities, items, natures and types',
    'hub.competitive.title': 'Competitive',
    'hub.competitive.subtitle': 'Tools to build and measure a team',
    'hub.pokedex.title': 'Pokedex',
    'hub.pokedex.subtitle': 'All 1025 species and the tools built on them',
    'hub.calculator.title': 'Calculator',
    'hub.calculator.subtitle': 'IV/EV, damage and capture',
```

- [ ] **Step 4: Escribir `js/tools.js`**

```js
// ===== THE TOOL TABLE =====
//
// One table feeds three things: the home cards, the list inside each hub, and
// which nav tab lights up. Adding a tool is one entry here, not three places
// that drift apart.
//
// `base` is the first segment of the route. It is what maps a detail page like
// #/moves/53 back to its category, which the path alone does not carry.

export const CATEGORIES = [
  { id: 'pokedex', route: '#/pokedex', label: 'nav.pokedex' },
  { id: 'data', route: '#/data', label: 'nav.data' },
  { id: 'competitive', route: '#/competitive', label: 'nav.competitive' },
  // The calculator is three tabs that already fit and already work: its tab
  // goes straight to the page instead of to a hub listing them.
  { id: 'calculator', route: '#/calculator', label: 'nav.calculator', direct: true },
];

export const TOOLS = [
  { id: 'pokedex', category: 'pokedex', route: '#/pokedex', base: 'pokedex', icon: '📖', label: 'nav.pokedex', desc: 'home.pokedex.desc' },

  { id: 'moves', category: 'data', route: '#/moves', base: 'moves', icon: '💥', label: 'nav.moves', desc: 'home.moves.desc' },
  { id: 'abilities', category: 'data', route: '#/abilities', base: 'abilities', icon: '✨', label: 'nav.abilities', desc: 'home.abilities.desc' },
  { id: 'items', category: 'data', route: '#/items', base: 'items', icon: '🎒', label: 'nav.items', desc: 'home.items.desc' },
  { id: 'natures', category: 'data', route: '#/natures', base: 'natures', icon: '🧬', label: 'nav.natures', desc: 'home.natures.desc' },
  { id: 'types', category: 'data', route: '#/types', base: 'types', icon: '⚡', label: 'nav.types', desc: 'home.types.desc' },

  { id: 'team', category: 'competitive', route: '#/team', base: 'team', icon: '🛡️', label: 'nav.team', desc: 'home.team.desc' },

  { id: 'ivev', category: 'calculator', route: '#/calculator', base: 'calculator', icon: '🔢', label: 'nav.calculator', desc: 'home.calculator.desc' },
  { id: 'damage', category: 'calculator', route: '#/calculator?tab=damage', base: 'calculator', icon: '⚔️', label: 'calc.tab.damage', desc: 'home.damage.desc' },
  { id: 'capture', category: 'calculator', route: '#/calculator?tab=catch', base: 'calculator', icon: '🥎', label: 'calc.tab.catch', desc: 'home.capture.desc' },
];

export const toolsIn = categoryId => TOOLS.filter(tool => tool.category === categoryId);

// Which tab to light up for a path. Detail routes (#/moves/53) resolve through
// their first segment, so a Pokemon's page keeps Pokedex lit.
//
// A hub route (#/data) belongs to no tool, so it falls back to the category id:
// otherwise the tab would go dark on the very page the tab leads to.
export function categoryOf(path) {
  const base = path.split('/').filter(Boolean)[0] || '';
  if (!base) return '';
  const tool = TOOLS.find(tool => tool.base === base);
  if (tool) return tool.category;
  return CATEGORIES.some(category => category.id === base) ? base : '';
}

// A category holding a single tool links straight to it: a hub with one entry is
// a click that shows nothing. It becomes a hub as soon as it has two.
export function targetOf(categoryId) {
  const category = CATEGORIES.find(c => c.id === categoryId);
  if (!category) return '#/';
  const tools = toolsIn(categoryId);
  if (category.direct || tools.length === 1) return tools[0]?.route || category.route;
  return category.route;
}
```

- [ ] **Step 5: Ejecutar el check hasta verlo pasar**

Run: `node scripts/check-tools.mjs`
Expected: PASS — `All checks passed`.

- [ ] **Step 6: Commit**

```bash
git add js/tools.js js/i18n.js scripts/check-tools.mjs
git -c commit.gpgsign=false commit -m "feat(nav): add the single tool table behind home, hubs and the nav"
```

---

### Task 2: La página de hub

**Files:**
- Create: `js/hub.js`
- Modify: `js/app.js` (import y dos ramas nuevas en `route()`)

**Interfaces:**
- Consumes: `CATEGORIES`, `toolsIn`, `TOOLS` de `js/tools.js` (Task 1).
- Produces: `renderHub(container, categoryId)` — pinta el listado de una
  categoría. No devuelve nada.

- [ ] **Step 1: Escribir `js/hub.js`**

Reutiliza `home-grid` y `home-card`, que ya tienen estilos: esto reorganiza, no
repinta.

```js
// ===== CATEGORY HUB =====
//
// The middle page of a category: its tools as cards. Same markup as the home
// grid, so it inherits the styles that already exist.
import { CATEGORIES, toolsIn } from './tools.js';
import { t } from './i18n.js';

export function renderHub(container, categoryId) {
  const category = CATEGORIES.find(c => c.id === categoryId);
  const tools = toolsIn(categoryId);

  container.innerHTML = `
    <div class="page-header">
      <h1>${t(`hub.${categoryId}.title`)}</h1>
      <p>${t(`hub.${categoryId}.subtitle`)}</p>
    </div>
    <div class="home-grid">
      ${tools.map(tool => `
        <a href="${tool.route}" class="home-card">
          <div class="icon">${tool.icon}</div>
          <div class="label">${t(tool.label)}</div>
          <div class="desc">${t(tool.desc)}</div>
        </a>
      `).join('')}
    </div>
  `;

  // A category route with nothing in it would render an empty grid and look
  // broken, so say so instead.
  if (!category || !tools.length) {
    container.innerHTML = `<div class="no-results"><div class="icon">❓</div><p>${t('common.notfound')}</p></div>`;
  }
}
```

- [ ] **Step 2: Enchufar las dos rutas en `js/app.js`**

Añadir el import junto a los demás, arriba del fichero:

```js
import { renderHub } from './hub.js';
```

Y en `route()`, justo antes de la rama `} else if (path === '/calculator') {`:

```js
    } else if (path === '/data') {
      renderHub(app, 'data');
    } else if (path === '/competitive') {
      renderHub(app, 'competitive');
```

- [ ] **Step 3: Comprobarlo en el navegador con recarga real**

```bash
node scripts/serve.mjs 8095
```

Abrir `http://localhost:8095/index.html#/data`, y **recargar de verdad** (F5, no
solo cambiar el hash). Esperado: cabecera "Datos" y cinco tarjetas —
Movimientos, Habilidades, Objetos, Naturalezas, Tipos — cada una con su icono y
su descripción. Hacer clic en Movimientos y confirmar que llega a `#/moves`.

Repetir en `#/competitive`: hoy tiene una sola herramienta, así que se ve una
tarjeta (Equipo). La pestaña lo llevará directo, pero la ruta existe.

Confirmar que la consola no tiene ni un error.

- [ ] **Step 4: Commit**

```bash
git add js/hub.js js/app.js
git -c commit.gpgsign=false commit -m "feat(nav): add the category hub page"
```

---

### Task 3: El home desde la tabla

**Files:**
- Modify: `js/home.js` (se reescribe entero, 65 líneas)

**Interfaces:**
- Consumes: `CATEGORIES`, `toolsIn` de `js/tools.js` (Task 1).
- Produces: `renderHome(container)` — misma firma de siempre.

- [ ] **Step 1: Reescribir `js/home.js`**

Las diez tarjetas dejan de estar a mano y salen de la tabla, agrupadas por
categoría y en el mismo orden que la barra.

```js
// ===== HOME PAGE =====
//
// Every tool has a card here, grouped by the same categories as the nav bar, so
// the app reads the same from the top as from the menu. The cards come from
// tools.js: this file decides how they look, not which ones there are.
import { CATEGORIES, toolsIn } from './tools.js';
import { t } from './i18n.js';

export function renderHome(container) {
  const groups = CATEGORIES.map(category => {
    const tools = toolsIn(category.id);
    if (!tools.length) return '';
    return `
      <h2 class="home-group">${t(`hub.${category.id}.title`)}</h2>
      <div class="home-grid">
        ${tools.map(tool => `
          <a href="${tool.route}" class="home-card">
            <div class="icon">${tool.icon}</div>
            <div class="label">${t(tool.label)}</div>
            <div class="desc">${t(tool.desc)}</div>
          </a>
        `).join('')}
      </div>
    `;
  }).join('');

  container.innerHTML = `
    <div class="home-hero">
      <div class="pokeball-icon-lg"></div>
      <h1>POKE<span class="accent">UTILS</span></h1>
      <p>${t('home.tagline')}</p>
    </div>
    ${groups}
  `;
}
```

- [ ] **Step 2: Dar estilo al encabezado de grupo**

En `style.css`, junto a `.home-grid`:

```css
.home-group {
  margin: 28px 0 0;
  font-size: 0.6rem;
  color: var(--text-dim);
  letter-spacing: 1px;
  text-transform: uppercase;
}
```

- [ ] **Step 3: Comprobarlo en el navegador**

Recargar `http://localhost:8095/index.html#/` de verdad. Esperado: **las diez
tarjetas siguen estando**, ahora bajo cuatro encabezados (Pokédex, Datos,
Competitivo, Calculadora). Ninguna tarjeta perdida y ninguna apuntando a una
ruta que no existe: hacer clic en las diez, una por una, y volver atrás.

Cambiar el idioma con el botón EN y confirmar que los encabezados y las
descripciones cambian, sin que aparezca ninguna clave cruda tipo `hub.data.title`.

- [ ] **Step 4: Commit**

```bash
git add js/home.js style.css
git -c commit.gpgsign=false commit -m "feat(nav): build the home cards from the tool table, grouped"
```

---

### Task 4: La barra de cuatro

**Files:**
- Modify: `index.html:30-40` (el bloque `.nav-links`)
- Modify: `js/app.js:51-67` (`updateNavLabels`) y `js/app.js:107-116` y
  `js/app.js:118-175` (`updateActiveNav` y las llamadas dentro de `route()`)

**Interfaces:**
- Consumes: `categoryOf`, `targetOf`, `CATEGORIES` de `js/tools.js` (Task 1).

- [ ] **Step 1: Dejar la barra en cuatro enlaces**

En `index.html`, sustituir el contenido de `<div class="nav-links" id="navLinks">`
por:

```html
        <a href="#/" class="nav-link" data-page="home">INICIO</a>
        <a href="#/pokedex" class="nav-link" data-page="pokedex">POKEDEX</a>
        <a href="#/data" class="nav-link" data-page="data">DATOS</a>
        <a href="#/competitive" class="nav-link" data-page="competitive">COMPETITIVO</a>
        <a href="#/calculator" class="nav-link" data-page="calculator">CALCULADORA</a>
```

Son cuatro categorías más Inicio. El `href` de cada categoría lo corrige el
siguiente paso: una categoría con una sola herramienta debe llevar directa a
ella.

- [ ] **Step 2: Que las etiquetas y los destinos salgan de la tabla**

En `js/app.js`, sustituir `updateNavLabels()` (líneas 51-67) por:

```js
function updateNavLabels() {
  document.querySelectorAll('.nav-link').forEach(link => {
    const page = link.dataset.page;
    if (page === 'home') {
      link.textContent = t('nav.home');
      return;
    }
    const category = CATEGORIES.find(c => c.id === page);
    if (!category) return;
    link.textContent = t(category.label);
    // A category with a single tool links straight to it; targetOf decides.
    link.setAttribute('href', targetOf(category.id));
  });
}
```

Y añadir el import arriba, junto a los demás:

```js
import { CATEGORIES, categoryOf, targetOf } from './tools.js';
```

- [ ] **Step 3: Encender la pestaña por categoría, no por página**

En `js/app.js`, sustituir `updateActiveNav()` por:

```js
// The tab that lights up is the tool's category, which the path does not carry:
// #/moves has to light up Datos. tools.js holds that map.
function updateActiveNav(path) {
  const active = path === '/' || path === '/home' ? 'home' : categoryOf(path);
  document.querySelectorAll('.nav-link').forEach(link => {
    link.classList.toggle('active', link.dataset.page === active);
  });
}
```

- [ ] **Step 4: Llamarla una sola vez en `route()`**

En `route()`, borrar **todas** las llamadas `updateActiveNav('...')` repartidas
por las ramas (`'home'`, `'types'`, `'team'`, `'pokedex'`, `'moves'`,
`'abilities'`, `'items'`, `'natures'`, `'calculator'` y la del `else` final), y
poner una sola justo después de `const { path, parts, query } = parseHash();`:

```js
  updateActiveNav(path);
```

- [ ] **Step 5: Comprobar los cinco casos en el navegador**

Recargar de verdad y confirmar qué pestaña queda encendida:

| URL | Pestaña encendida |
|---|---|
| `#/` | Inicio |
| `#/moves` y `#/moves/53` | Datos |
| `#/pokedex/6` | Pokédex |
| `#/team` | Competitivo |
| `#/calculator?tab=damage` | Calculadora |

Y que el enlace de la pestaña Competitivo lleva a `#/team` directamente, porque
hoy es su única herramienta.

- [ ] **Step 6: Commit**

```bash
git add index.html js/app.js
git -c commit.gpgsign=false commit -m "fix(nav): light up the tab by category instead of by page"
```

---

### Task 5: Verificación completa

**Files:**
- Ninguno que crear. Solo se corrige lo que falle.

- [ ] **Step 1: Los scripts de comprobación siguen en verde**

```bash
node scripts/check-tools.mjs
node scripts/check-damage-url.mjs
node scripts/check-variable-power.mjs
node scripts/check-damage.mjs
node scripts/check-capture.mjs
```

Expected: los cinco terminan en `All checks passed`.

- [ ] **Step 2: Las 12 rutas responden**

Con el servidor levantado y **recargando de verdad** en cada una:

`#/`, `#/pokedex`, `#/pokedex/6`, `#/moves`, `#/moves/53`, `#/abilities`,
`#/abilities/blaze`, `#/items`, `#/natures`, `#/types`, `#/team`, `#/calculator`,
más las dos nuevas `#/data` y `#/competitive`.

Ninguna puede caer en la pantalla de "no encontrado".

- [ ] **Step 3: El estado en la URL sobrevive**

Este es el que de verdad importa, porque es lo que se comparte:

```
#/calculator?tab=damage&a=6&d=3&m=53&al=100&crit=1
```

Esperado: **168 - 198**, con la casilla de crítico marcada. Es el mismo valor que
daba antes de tocar la navegación.

Y `#/team?ids=3,6,9` tiene que seguir cargando sus tres miembros.

- [ ] **Step 4: La barra ya no se corta**

Con las herramientas de desarrollo, poner el ancho en **1018 px** y comprobar que
los cinco enlaces caben en una línea, que es donde se cortaba con nueve. Repetir
a **360 px** y confirmar que el menú móvil abre y cierra.

- [ ] **Step 5: Paridad hub ↔ home**

Recorrer el home: las diez herramientas tienen tarjeta con icono y descripción.
Recorrer `#/data`: sus cinco están listadas. Ninguna herramienta existe sin
tarjeta.

- [ ] **Step 6: Consola limpia**

Cero errores y cero avisos en toda la sesión de pruebas.

- [ ] **Step 7: Commit de lo que se haya corregido**

Si algo falló y se arregló, va en su propio commit:

```bash
git -c commit.gpgsign=false commit -m "fix(nav): <lo que fallaba>"
```

---

## Lo que este plan NO hace

- **No crea ninguna herramienta nueva.** Comparador, grupos huevo, velocidad,
  ¿sobrevive? y contrarrestar llegan en el sub-bloque 4.
- **No toca datos** ni el builder.
- **No repinta nada.** Reutiliza `home-grid`, `home-card` y `page-header`.
- **No convierte Pokédex ni Competitivo en hubs**: hoy tienen una herramienta
  cada una. Pasarán a serlo en el sub-bloque 4, y entonces las dos
  comprobaciones de `targetOf` en `check-tools.mjs` cambian a `#/pokedex` y
  `#/competitive`.
