# Only headers here on purpose: the build and publish settings stay in the
# Netlify UI, and this file must not override them.

# Generated datasets change once per Pokemon generation. Serve them stale
# while revalidating so navigation stays instant after the first visit.
[[headers]]
  for = "/data/*.json"
  [headers.values]
    Cache-Control = "public, max-age=3600, stale-while-revalidate=604800"
